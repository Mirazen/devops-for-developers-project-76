---
- name: Prepare web servers
  hosts: all
  become: true

  pre_tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
      when: ansible_os_family == 'Debian'

  roles:
    - geerlingguy.pip
    - geerlingguy.docker
